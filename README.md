# Diamon Square Solutions Website

This repository contains the static website and the Node.js contact form backend.

## Local development

1. Copy `.env.example` to `.env`
2. Fill in your Zoho SMTP credentials
3. Install dependencies:
   `npm install`
4. Start the server:
   `npm start`
